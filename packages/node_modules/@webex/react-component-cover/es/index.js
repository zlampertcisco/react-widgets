import React from 'react';
import PropTypes from 'prop-types';
import classNames from 'classnames';

function styleInject(css, ref) {
  if (ref === void 0) ref = {};
  var insertAt = ref.insertAt;

  if (!css || typeof document === 'undefined') {
    return;
  }

  var head = document.head || document.getElementsByTagName('head')[0];
  var style = document.createElement('style');
  style.type = 'text/css';

  if (insertAt === 'top') {
    if (head.firstChild) {
      head.insertBefore(style, head.firstChild);
    } else {
      head.appendChild(style);
    }
  } else {
    head.appendChild(style);
  }

  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
}

var css_248z = ".react-component-cover__message__Lm1lc3Nh {\n  position: absolute;\n  z-index: 1000;\n  display: none;\n  width: 100%;\n  height: 100%;\n  margin: auto;\n  text-align: center;\n  background: rgba(0, 0, 0, 0.6);\n  align-items: center;\n}\n\n.react-component-cover__title__Lm1lc3Nh {\n  width: 100%;\n  font-size: 32px;\n  color: #fff;\n}\n";
var styles = {"message":"react-component-cover__message__Lm1lc3Nh","title":"react-component-cover__title__Lm1lc3Nh"};
styleInject(css_248z);

const propTypes = {
  message: PropTypes.string.isRequired
};

function Cover({
  message
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: classNames('webex-cover-message', styles.message)
  }, /*#__PURE__*/React.createElement("div", {
    className: classNames('webex-cover-message-title', styles.title)
  }, message));
}

Cover.propTypes = propTypes;

export default Cover;
//# sourceMappingURL=index.js.map
