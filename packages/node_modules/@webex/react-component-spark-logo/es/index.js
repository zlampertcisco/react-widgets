import React from 'react';
import classNames from 'classnames';

function styleInject(css, ref) {
  if (ref === void 0) ref = {};
  var insertAt = ref.insertAt;

  if (!css || typeof document === 'undefined') {
    return;
  }

  var head = document.head || document.getElementsByTagName('head')[0];
  var style = document.createElement('style');
  style.type = 'text/css';

  if (insertAt === 'top') {
    if (head.firstChild) {
      head.insertBefore(style, head.firstChild);
    } else {
      head.appendChild(style);
    }
  } else {
    head.appendChild(style);
  }

  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
}

var css_248z = ".react-component-spark-logo__logo__LmxvZ28g {\n  width: 100%;\n  height: 100%;\n  min-height: 60px;\n  background: transparent url('./logo-spark.png') no-repeat;\n  background-position: center;\n  background-size: contain;\n}\n";
var styles = {"logo":"react-component-spark-logo__logo__LmxvZ28g"};
styleInject(css_248z);

function SparkLogo() {
  return /*#__PURE__*/React.createElement("div", {
    className: classNames('webex-spark-logo', styles.logo)
  });
}

export default SparkLogo;
//# sourceMappingURL=index.js.map
