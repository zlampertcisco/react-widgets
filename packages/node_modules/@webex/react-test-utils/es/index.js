import React from 'react';
import renderer from 'react-test-renderer';
import ShallowRenderer from 'react-test-renderer/shallow';
import { IntlProvider } from 'react-intl';

function createComponentWithIntl(children, props = {
  locale: 'en'
}) {
  return renderer.create( /*#__PURE__*/React.createElement(IntlProvider, props, children));
}

function createShallowComponentWithIntl(children, props = {
  locale: 'en'
}) {
  const shallowRenderer = new ShallowRenderer();
  shallowRenderer.render( /*#__PURE__*/React.createElement(IntlProvider, props, children));
  return shallowRenderer.getRenderOutput();
}

export { createComponentWithIntl, createShallowComponentWithIntl };
//# sourceMappingURL=index.js.map
